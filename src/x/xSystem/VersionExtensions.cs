namespace System {
  public static class VersionExtensions {

    //public static DateTime BuildDate(this Version v) => new DateTime(2000, 1, 1).AddDays(v.Build).AddSeconds(v.Revision * 2);

    //public static DateTime BuildDate2(this Version v) => new DateTime((v.Build - 1) * TimeSpan.TicksPerDay + v.Revision * TimeSpan.TicksPerSecond * 2).AddYears(1999);

  }
}